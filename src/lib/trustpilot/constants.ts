/** Fallback rating specified for PhoneSpot's public storefront. */
export const TRUSTPILOT_FALLBACK_SCORE = 4.7;
export const TRUSTPILOT_SCORE_LABEL = TRUSTPILOT_FALLBACK_SCORE.toFixed(1);
export const TRUSTPILOT_SCORE_LABEL_DA = TRUSTPILOT_SCORE_LABEL.replace(".", ",");
